apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceName }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: gc-operator
  ports:
    - protocol: TCP
      port: {{ .Values.controllerPort }}