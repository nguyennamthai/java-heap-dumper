apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Release.Namespace }}
spec:
  selector: {{ toYaml .Values.labels | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.controllerPort }}